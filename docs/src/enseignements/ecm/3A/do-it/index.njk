---
layout: layout/post.njk 
title: Enseignements dans le parcours do-it

tags: ['enseignement', 'ECM']

eleventyNavigation:
  key: "parcours doit"
  parent: 3A
  order: 1

---

<!-- début résumé -->

Enseignements donnés dans le parcours do-it.

<!-- fin résumé -->

<p>
  {% prerequis "<strong>Prérequis :</strong>" %}

  <ul>
    <li>
      <a href="{{ '/tutoriels/installation-système' | url }}">Avoir un système en état de marche</a>
    </li>
    <li>
      <a href="{{ '/tutoriels/fichiers-navigation' | url }}">Savoir naviguer dans un système de fichiers</a>
    </li>
    <li>
      <a href="{{ '/tutoriels/vsc-installation-et-prise-en-main' | url }}">Installation et prise en main de l'éditeur de texte vsc</a>
    </li>
    <li>pourra être très utile :
    <ul>
        <li>
          <a href="{{ '/tutoriels/terminal' | url }}">Savoir ouvrir une fenêtre terminal</a>
        </li>
        <li>
          <a href="{{ '/tutoriels/brew' | url }}">Si vous êtes sous mac, installez brew</a>
        </li>
      </ul>
    </li>
  </ul>

  {% endprerequis %}
</p>

{% set post = "" %}
{% for page in collections.enseignement | eleventyNavigation(eleventyNavigation.key) %}
  {% for index in collections.enseignement %}
    {% if index.url == page.url %}
      {% set post = index %}
    {% endif %}
  {% endfor %}

  {% if post.data.tags %}
    <div class="my-3 px-2 p-1 border-solid border-2 rounded">
      <div class="text-3xl mb-3">
        <a href="{{ post.url | url }}">{{ post.data.title }}</a>
      </div>
      <div class=" px-4 flex items-center">
        <div>Tags : </div>
        <ul class="flex not-prose list-none my-0 last:after:content-['•'] last:after:px-1 mx-0 px-1">
          {% for tag in post.data.tags | sort %}
            {% if tag != "cours" %}
              <li class="before:content-['•'] before:px-1">{{ tag }}</li>
            {% endif %}
          {% endfor %}
        </ul>
      </div>
      <div class=" pt-1 px-4 flex items-top">
        <div class="flex-none">Résumé : </div>
        <div class="pl-2 m-0 grow quote">

          {% résumé post %}

        </div>
      </div>

    </div>

  {% endif %}
{% endfor %}