---
layout: layout/post.njk

title: Installation de Linux/Ubuntu

eleventyComputed:
  eleventyNavigation:
    key: "{{ page.url }}"
    title: "{{ title | safe }}"
    parent: "{{ '../' | siteUrl(page.url) }}"
---


* sur une VM
* sur son disque dur
* wsl et windows
